# エージェント定義 (AGENTS.md)

このドキュメントでは、Discordボット「Crok」を構成する主要なコンポーネント（エージェント）の役割と責任について定義します。

## 🤖 Crok Bot (Main Application)

**担当ファイル**: [`src/index.ts`](src/index.ts)

システムの中心となるエージェントであり、Discordクライアントのライフサイクルを管理します。

**主な役割**:

- **イベントハンドリング**: `MessageCreate` や `VoiceStateUpdate` などのDiscordイベントを監視。
- **機能の統合**: 俳句検出機能やVC管理機能を利用してユーザーアクションに応答。
- **ボットの振る舞い**:
    - **俳句応答**: ユーザーのメッセージから俳句を検出した場合、整形して返信。
    - **VC自動入退室**: ユーザーがVCに参加した際に付随して参加し、誰もいなくなると退出。
    - **メンション応答**: ボットへのメンションに対して特定のリアクション（現在はランダム絵文字）を返す。

## 🎋 Haiku Detector (Service)

**担当ファイル**: [`src/haiku/HaikuDetector.ts`](src/haiku/HaikuDetector.ts)

テキスト解析と俳句判定を行う専門家エージェントです。高度なモーラ計算ロジックを持ちます。

**主な役割**:

- **形態素解析**: `kuromoji` を使用して日本語テキストをトークン化。
- **モーラ計算**:
    - 拗音（きゃ、きゅ、きょ等）、長音（ー）、促音（ッ）、撥音（ン）を考慮した正確な拍数カウント。
    - **二重母音調整**: 「〜アイ」「〜オウ」などの二重母音を、実際の発話感覚に合わせてモーラ数を調整（例：「ナイ」を2モーラではなく1モーラ相当として扱う場合がある）。
- **英語対応**: 英単語が含まれる場合、辞書またはアルファベット読みを使用してカタカナに変換し、モーラ数を算出。
- **記号処理**: マークダウン記号や括弧のペアリング処理、無効な記号を含むテキストの除外。
- **5-7-5検出**: スライディングウィンドウ方式でテキスト内から5-7-5のパターンを探索。

## 📖 English Dictionary (Resource)

**担当ファイル**: [`src/haiku/englishDictionary.ts`](src/haiku/englishDictionary.ts)

Haiku Detectorを支援する知識ベースです。

**主な役割**:

- **英単語変換**: 一般的な英単語やプログラミング用語を、自然なカタカナ読みにマッピングする静的データを提供。
- **Haiku Detectorへのデータ提供**: 検出器が未知の英単語に遭遇した際、この辞書を参照して適切な読み（モーラ数）を決定できるようにする（辞書にない場合はアルファベット読みでフォールバック）。

tsxで検査してください。
